<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:anroid="http://schemas.android.com/apk/res-auto"

    tools:showIn="navigation_view">

    <group android:checkableBehavior="single"
        android:id="@+id/menu_top">
        <item
            android:id="@+id/nav_camera"
            android:checked="true"
            android:icon="@drawable/camera_icon_grey"
            android:title="Upload Selfie" />

    </group>

    <group
        android:id="@+id/menu_bottom"
        android:checkableBehavior="none">

        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/home_icon"
            android:checked="true"
            android:title="Home"/>

        <item
            android:id="@+id/nav_games"
            android:icon="@drawable/encounter_icon"
            android:checked="true"
            android:title="Encounter" />
        <item
            android:id="@+id/nav_redeem"
            android:icon="@drawable/earn_coin"
            android:checked="true"
            android:title="Earn Coins" />
        <item
            android:id="@+id/leader_board"
            android:icon="@drawable/top_rank"
            android:checked="true"
            android:title="Leaderboard" />
        <item
            android:id="@+id/nav_event"
            android:icon="@drawable/events_notification"
            android:checked="true"
            android:title="Event Notification" />
        <item
            android:id="@+id/nav_account"
            android:icon="@drawable/profile_icon"
            android:checked="true"
            android:title="Profile" />

    </group>
</menu>
